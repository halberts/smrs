@echo -----------------------------------------------------------------------------
@echo 测试环境将hop-core${version}.jar,hop-test${Version}.jar发布到maven仓库中
@echo -----------------------------------------------------------------------------
mvn deploy -Ptest
@pause