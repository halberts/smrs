@echo -----------------------------------------------------------------------------
@echo 开发环境将hop-core${version}.jar,hop-test${Version}.jar发布到maven仓库中
@echo -----------------------------------------------------------------------------
mvn clean deploy -Pdev
@pause